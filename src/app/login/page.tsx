import LoginForm from "@/frontend/components/auth/LoginForm";

export const metadata = {
  title: "Portal Login",
  description: "Secure blue-themed login portal for students, parents, and faculty at MNRS.",
};

export default function LoginPage() {
  return <LoginForm />;
}
